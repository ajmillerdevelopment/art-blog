<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <title>Collab Blog</title>
</head>
<body>
    <%- include('../partials/header') %>
    <main>
        <h1>Collaborative Blog</h1>

        <div>
            <% for (let i = 0; i < posts.length; i++) { %>
                <% if (posts[i].crosspost) { %>
                    <h3><%= posts[i].title %></h3>
                    <p><%= posts[i].body %></p>
                    <br />
                    <p><%= posts[i].editedAt %></p>
                <% } %>
             <% } %>
        </div>
        
    </main>
</body>
</html>