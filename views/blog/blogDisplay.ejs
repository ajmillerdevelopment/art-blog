<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/blog.css">
    <title><%= post.title %></title>
</head>
<body>
    <%- include('../partials/header') %>
        <h2><%= post.title %></h2>
        <h3>By: <a href="/users/<%= post.author._id %> "><%= post.author.displayName %></a></h3> 
        <h6><%= post.updatedAt %></h6>
        <article class="box">
            <p><%= post.body %></p>
            <% for (let i = 0; i < post.images.length; i++) { %>
            <img src="<%= post.images[i] %> " alt="<%= post.captions[i] %> ">
            <% } %>
        </article>
</body>
</html>